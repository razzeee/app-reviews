You need to have deno installed. See: https://deno.land/#installation

Then install `vr` to be able to use our script runner.

```bash
deno install -qAn vr https://deno.land/x/velociraptor@1.0.1/cli.ts

vr
```
